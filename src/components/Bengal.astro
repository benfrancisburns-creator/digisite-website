---
// Bengal.astro — DigiSite AI chat assistant
---

<div id="bengal-root">
  <!-- Bubble button -->
  <button
    id="bengal-toggle"
    aria-label="Open Bengal chat"
    class="bengal-bubble"
  >
    <svg id="bengal-icon-chat" xmlns="http://www.w3.org/2000/svg" width="26" height="26" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
      <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
    </svg>
    <svg id="bengal-icon-close" xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="display:none">
      <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
    </svg>
  </button>

  <!-- Notification dot -->
  <span id="bengal-dot" class="bengal-dot"></span>

  <!-- Chat window -->
  <div id="bengal-window" class="bengal-window" aria-hidden="true">
    <!-- Header -->
    <div class="bengal-header">
      <div class="bengal-header-left">
        <div class="bengal-avatar">B</div>
        <div>
          <div class="bengal-name">Bengal</div>
          <div class="bengal-status">
            <span class="bengal-online-dot"></span> Online
          </div>
        </div>
      </div>
      <button id="bengal-close-btn" aria-label="Close chat" class="bengal-close-btn">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>

    <!-- Messages -->
    <div id="bengal-messages" class="bengal-messages"></div>

    <!-- Input area -->
    <div class="bengal-input-area">
      <input
        id="bengal-input"
        type="text"
        placeholder="Ask me anything…"
        autocomplete="off"
        maxlength="500"
        class="bengal-input"
      />
      <button id="bengal-send" aria-label="Send" class="bengal-send-btn">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round">
          <line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/>
        </svg>
      </button>
    </div>

    <!-- Footer -->
    <div class="bengal-footer">Powered by Bengal AI</div>
  </div>
</div>

<style>
  /* ── Layout ─────────────────────────────────────── */
  #bengal-root {
    position: fixed;
    bottom: 28px;
    right: 28px;
    z-index: 9999;
    font-family: 'Inter', system-ui, sans-serif;
  }

  /* ── Bubble button ───────────────────────────────── */
  .bengal-bubble {
    width: 58px;
    height: 58px;
    border-radius: 50%;
    background: linear-gradient(135deg, #00D4FF 0%, #0099CC 100%);
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0D1B2A;
    box-shadow: 0 4px 20px rgba(0, 212, 255, 0.45);
    transition: transform 0.2s ease, box-shadow 0.2s ease;
    position: relative;
    z-index: 2;
  }
  .bengal-bubble:hover {
    transform: scale(1.08);
    box-shadow: 0 6px 28px rgba(0, 212, 255, 0.6);
  }

  /* ── Notification dot ────────────────────────────── */
  .bengal-dot {
    position: absolute;
    top: 0;
    right: 0;
    width: 14px;
    height: 14px;
    background: #F5A623;
    border: 2px solid #fff;
    border-radius: 50%;
    display: block;
    animation: bengal-pulse 2s infinite;
  }
  .bengal-dot.hidden { display: none; }
  @keyframes bengal-pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.2); }
  }

  /* ── Chat window ─────────────────────────────────── */
  .bengal-window {
    position: absolute;
    bottom: 72px;
    right: 0;
    width: 360px;
    max-height: 520px;
    background: #0F2035;
    border-radius: 16px;
    border: 1px solid rgba(0, 212, 255, 0.15);
    box-shadow: 0 12px 48px rgba(0, 0, 0, 0.4), 0 0 24px rgba(0, 212, 255, 0.08);
    display: flex;
    flex-direction: column;
    overflow: hidden;
    opacity: 0;
    transform: translateY(12px) scale(0.97);
    pointer-events: none;
    transition: opacity 0.22s ease, transform 0.22s ease;
  }
  .bengal-window.open {
    opacity: 1;
    transform: translateY(0) scale(1);
    pointer-events: all;
  }

  /* ── Header ──────────────────────────────────────── */
  .bengal-header {
    background: #0D1B2A;
    padding: 14px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-shrink: 0;
  }
  .bengal-header-left {
    display: flex;
    align-items: center;
    gap: 10px;
  }
  .bengal-avatar {
    width: 36px;
    height: 36px;
    border-radius: 50%;
    background: linear-gradient(135deg, #00D4FF, #0077AA);
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    font-size: 15px;
    color: #0D1B2A;
    flex-shrink: 0;
  }
  .bengal-name {
    font-weight: 700;
    font-size: 14px;
    color: #ffffff;
    letter-spacing: 0.02em;
  }
  .bengal-status {
    font-size: 11px;
    color: #8A9BB0;
    display: flex;
    align-items: center;
    gap: 4px;
    margin-top: 1px;
  }
  .bengal-online-dot {
    width: 7px;
    height: 7px;
    background: #22c55e;
    border-radius: 50%;
    display: inline-block;
  }
  .bengal-close-btn {
    background: none;
    border: none;
    color: #8A9BB0;
    cursor: pointer;
    padding: 4px;
    border-radius: 6px;
    display: flex;
    align-items: center;
    transition: color 0.15s;
  }
  .bengal-close-btn:hover { color: #ffffff; }

  /* ── Messages ────────────────────────────────────── */
  .bengal-messages {
    flex: 1;
    overflow-y: auto;
    padding: 16px;
    display: flex;
    flex-direction: column;
    gap: 10px;
    scroll-behavior: smooth;
    background: #0F2035;
  }
  .bengal-messages::-webkit-scrollbar { width: 4px; }
  .bengal-messages::-webkit-scrollbar-track { background: transparent; }
  .bengal-messages::-webkit-scrollbar-thumb { background: #2A3F52; border-radius: 4px; }

  /* Message bubbles */
  .bengal-msg {
    max-width: 84%;
    padding: 10px 14px;
    border-radius: 14px;
    font-size: 13.5px;
    line-height: 1.5;
    word-break: break-word;
    animation: bengal-fadein 0.18s ease;
  }
  @keyframes bengal-fadein {
    from { opacity: 0; transform: translateY(6px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .bengal-msg.bot {
    background: #162236;
    color: #F7F9FC !important;
    border: 1px solid rgba(0, 212, 255, 0.12);
    border-bottom-left-radius: 4px;
    align-self: flex-start;
  }
  .bengal-msg.user {
    background: #00D4FF;
    color: #0D1B2A !important;
    border-bottom-right-radius: 4px;
    align-self: flex-end;
  }

  /* Typing indicator */
  .bengal-typing {
    display: flex;
    align-items: center;
    gap: 4px;
    padding: 10px 14px;
    background: #162236;
    border: 1px solid rgba(0, 212, 255, 0.12);
    border-radius: 14px;
    border-bottom-left-radius: 4px;
    align-self: flex-start;
  }
  .bengal-typing span {
    width: 6px;
    height: 6px;
    background: #8A9BB0;
    border-radius: 50%;
    animation: bengal-bounce 1.2s infinite;
  }
  .bengal-typing span:nth-child(2) { animation-delay: 0.2s; }
  .bengal-typing span:nth-child(3) { animation-delay: 0.4s; }
  @keyframes bengal-bounce {
    0%, 60%, 100% { transform: translateY(0); }
    30% { transform: translateY(-5px); }
  }

  /* Suggested questions */
  .bengal-suggestions {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 4px;
    align-self: flex-start;
  }
  .bengal-suggestion-btn {
    background: rgba(0, 212, 255, 0.08);
    border: 1px solid rgba(0, 212, 255, 0.25);
    color: #00D4FF;
    border-radius: 20px;
    padding: 5px 12px;
    font-size: 12px;
    cursor: pointer;
    transition: background 0.15s, border-color 0.15s;
    font-family: inherit;
  }
  .bengal-suggestion-btn:hover {
    background: rgba(0, 212, 255, 0.15);
    border-color: #00D4FF;
  }

  /* ── Input area ──────────────────────────────────── */
  .bengal-input-area {
    display: flex;
    align-items: center;
    gap: 8px;
    padding: 12px 14px;
    background: #0D1B2A;
    border-top: 1px solid rgba(0, 212, 255, 0.1);
    flex-shrink: 0;
  }
  .bengal-input {
    flex: 1;
    border: 1.5px solid #2A3F52;
    border-radius: 10px;
    padding: 9px 12px;
    font-size: 13.5px;
    font-family: inherit;
    color: #F7F9FC;
    background: #0F2035;
    outline: none;
    transition: border-color 0.15s;
  }
  .bengal-input:focus { border-color: #00D4FF; background: #162236; }
  .bengal-input::placeholder { color: #8A9BB0; }
  .bengal-send-btn {
    width: 36px;
    height: 36px;
    border-radius: 10px;
    background: #00D4FF;
    border: none;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    color: #0D1B2A;
    flex-shrink: 0;
    transition: background 0.15s;
  }
  .bengal-send-btn:hover { background: #33DFFF; }
  .bengal-send-btn:disabled { opacity: 0.4; cursor: not-allowed; }

  /* ── Footer ──────────────────────────────────────── */
  .bengal-footer {
    text-align: center;
    font-size: 10.5px;
    color: #8A9BB0;
    padding: 6px 0 8px;
    background: #0D1B2A;
    border-top: 1px solid rgba(0, 212, 255, 0.08);
    flex-shrink: 0;
  }

  /* ── Mobile ──────────────────────────────────────── */
  @media (max-width: 420px) {
    #bengal-root { bottom: 16px; right: 16px; }
    .bengal-window { width: calc(100vw - 32px); right: 0; }
  }
</style>

<script>
  const GREETING = "Hi there 👋 I'm Bengal, DigiSite's AI assistant. I can tell you about our advertising screens, partnership schemes, or revenue potential. What would you like to know?";

  const SUGGESTIONS = [
    "How does the revenue model work?",
    "We're a scaffold company — how do we partner?",
    "Tell me about investor opportunity",
    "What's DigiPerim?",
  ];

  const toggle = document.getElementById('bengal-toggle');
  const window_ = document.getElementById('bengal-window');
  const closeBtn = document.getElementById('bengal-close-btn');
  const input = document.getElementById('bengal-input');
  const sendBtn = document.getElementById('bengal-send');
  const messagesEl = document.getElementById('bengal-messages');
  const iconChat = document.getElementById('bengal-icon-chat');
  const iconClose = document.getElementById('bengal-icon-close');
  const dot = document.getElementById('bengal-dot');

  let isOpen = false;
  let isTyping = false;
  let hasOpened = false;
  let conversationHistory = [];

  function openChat() {
    isOpen = true;
    window_.classList.add('open');
    window_.setAttribute('aria-hidden', 'false');
    iconChat.style.display = 'none';
    iconClose.style.display = 'block';
    dot.classList.add('hidden');
    if (!hasOpened) {
      hasOpened = true;
      appendBotMessage(GREETING, true);
    }
    setTimeout(() => input.focus(), 220);
  }

  function closeChat() {
    isOpen = false;
    window_.classList.remove('open');
    window_.setAttribute('aria-hidden', 'true');
    iconChat.style.display = 'block';
    iconClose.style.display = 'none';
  }

  toggle.addEventListener('click', () => isOpen ? closeChat() : openChat());
  closeBtn.addEventListener('click', closeChat);

  function scrollToBottom() {
    messagesEl.scrollTop = messagesEl.scrollHeight;
  }

  function appendBotMessage(text, showSuggestions = false) {
    const msg = document.createElement('div');
    msg.className = 'bengal-msg bot';
    msg.textContent = text;
    messagesEl.appendChild(msg);

    if (showSuggestions) {
      const suggestionsEl = document.createElement('div');
      suggestionsEl.className = 'bengal-suggestions';
      SUGGESTIONS.forEach(q => {
        const btn = document.createElement('button');
        btn.className = 'bengal-suggestion-btn';
        btn.textContent = q;
        btn.addEventListener('click', () => {
          suggestionsEl.remove();
          handleSend(q);
        });
        suggestionsEl.appendChild(btn);
      });
      messagesEl.appendChild(suggestionsEl);
    }

    scrollToBottom();
  }

  function appendUserMessage(text) {
    const msg = document.createElement('div');
    msg.className = 'bengal-msg user';
    msg.textContent = text;
    messagesEl.appendChild(msg);
    scrollToBottom();
  }

  function showTyping() {
    const el = document.createElement('div');
    el.className = 'bengal-typing';
    el.id = 'bengal-typing-indicator';
    el.innerHTML = '<span></span><span></span><span></span>';
    messagesEl.appendChild(el);
    scrollToBottom();
  }

  function removeTyping() {
    const el = document.getElementById('bengal-typing-indicator');
    if (el) el.remove();
  }

  async function handleSend(overrideText) {
    const text = overrideText || input.value.trim();
    if (!text || isTyping) return;

    input.value = '';
    appendUserMessage(text);
    conversationHistory.push({ role: 'user', content: text });

    isTyping = true;
    sendBtn.disabled = true;
    showTyping();

    try {
      const res = await fetch('https://bengal-chat.benfrancisburns.workers.dev', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ messages: conversationHistory }),
      });

      const data = await res.json();
      const reply = data.reply || "Sorry, something went wrong. Please try again.";

      removeTyping();
      appendBotMessage(reply);
      conversationHistory.push({ role: 'assistant', content: reply });

    } catch (err) {
      removeTyping();
      appendBotMessage("I'm having trouble connecting right now. Please try again in a moment.");
    }

    isTyping = false;
    sendBtn.disabled = false;
    input.focus();
  }

  sendBtn.addEventListener('click', () => handleSend());

  input.addEventListener('keydown', (e) => {
    if (e.key === 'Enter' && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  });

  // Auto-open after 4 seconds on first visit
  setTimeout(() => {
    if (!isOpen) openChat();
  }, 4000);
</script>
